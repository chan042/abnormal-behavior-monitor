# Data Layout

- `samples/`: local sample videos for pipeline development
- `models/`: local model assets such as MediaPipe `.task` bundles
- `manifests/`: metadata files for test scenarios and replay runs

Real CCTV or evaluation assets should be placed here, but large binary files
should not be committed to Git.
